services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: secureops
      POSTGRES_PASSWORD: secureops
      POSTGRES_DB: secureops
    ports:
      - "5435:5432"
    volumes:
      - secureops_pg:/var/lib/postgresql/data

volumes:
  secureops_pg:
